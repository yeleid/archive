# archive
An archive app based on CDH

clone code repository:

    git clone https://github.com/yeleid/simplemorphline

compile and package:

    mvn clean package
    
run archive service:

    bin/archive.sh
    
    